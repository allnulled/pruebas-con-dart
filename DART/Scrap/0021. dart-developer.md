# dart-developer

Estás en el nodo 20 de 161 [12%]

## Índice de dart-developer

- Estructura de la documentación:
   - 20.1.0. dart-developer » Classes » Flow @6660
   - 20.1.1. dart-developer » Classes » NativeRuntime @6661
   - 20.1.2. dart-developer » Classes » Service @6662
   - 20.1.3. dart-developer » Classes » ServiceExtensionResponse @6663
   - 20.1.4. dart-developer » Classes » ServiceProtocolInfo @6664
   - 20.1.5. dart-developer » Classes » Timeline @6665
   - 20.1.6. dart-developer » Classes » TimelineTask @6666
   - 20.1.7. dart-developer » Classes » UserTag @6667
   - 20.2.0. dart-developer » Enums » TimelineRecorder @6668
   - 20.2.1. dart-developer » Enums » TimelineStream @6669
   - 20.3.0. dart-developer » Properties » extensionStreamHasListener → bool @6670
   - 20.3.1. dart-developer » Properties » reachabilityBarrier → int @6671
   - 20.4.0. dart-developer » Functions » debugger({bool when = true, String? message}) → bool @6672
   - 20.4.1. dart-developer » Functions » getCurrentTag() → UserTag @6673
   - 20.4.2. dart-developer » Functions » inspect(Object? object) → Object? @6674
   - 20.4.3. dart-developer » Functions » log(String message, {DateTime? time, int? sequenceNumber, int level = 0, String name = '', Zone? zone, Object? error, StackTrace? stackTrace}) → void @6675
   - 20.4.4. dart-developer » Functions » postEvent(String eventKind, Map eventData, {String stream = 'Extension'}) → void @6676
   - 20.4.5. dart-developer » Functions » registerExtension(String method, ServiceExtensionHandler handler) → void @6677
   - 20.5.0. dart-developer » Typedefs » ServiceExtensionHandler = Future&lt;ServiceExtensionResponse&gt; Function(String method, Map&lt;String, String&gt; parameters) @6678
   - 20.5.1. dart-developer » Typedefs » TimelineAsyncFunction = Future Function() @6679
   - 20.5.2. dart-developer » Typedefs » TimelineSyncFunction&lt;T&gt; = T Function() @6680
### 20.1.0. dart-developer » Classes » Flow @6660

> A class to represent Flow events.

### 20.1.1. dart-developer » Classes » NativeRuntime @6661

> Functionality available on the native runtime.

### 20.1.2. dart-developer » Classes » Service @6662

> Access information about the service protocol and control the web server that provides access to the services provided by the Dart VM for debugging and inspecting Dart programs.

### 20.1.3. dart-developer » Classes » ServiceExtensionResponse @6663

> A response to a service protocol extension RPC.

### 20.1.4. dart-developer » Classes » ServiceProtocolInfo @6664

> Service protocol is the protocol that a client like the Observatory could use to access the services provided by the Dart VM for debugging and inspecting Dart programs. This class encapsulates the version number and Uri for accessing this service.

### 20.1.5. dart-developer » Classes » Timeline @6665

> Add to the timeline.

### 20.1.6. dart-developer » Classes » TimelineTask @6666

> An asynchronous task on the timeline. An asynchronous task can have many (nested) synchronous operations. Synchronous operations can live longer than the current isolate event. To pass a TimelineTask to another isolate, you must first call pass to get the task id and then construct a new TimelineTask in the other isolate.

### 20.1.7. dart-developer » Classes » UserTag @6667

> A UserTag can be used to group samples in the DevTools CPU profiler.

### 20.2.0. dart-developer » Enums » TimelineRecorder @6668

> Types of timeline recorders supported by the VM.

### 20.2.1. dart-developer » Enums » TimelineStream @6669

> Specific sets of events whose recording can be enabled separately.

### 20.3.0. dart-developer » Properties » extensionStreamHasListener → bool @6670

> Whether the "Extension" stream currently has at least one listener. no setter

### 20.3.1. dart-developer » Properties » reachabilityBarrier → int @6671

> Current reachability barrier state. no setter

### 20.4.0. dart-developer » Functions » debugger({bool when = true, String? message}) → bool @6672

> If when is true, stop the program as if a breakpoint were hit at the following statement.

### 20.4.1. dart-developer » Functions » getCurrentTag() → UserTag @6673

> Returns the current UserTag for the isolate.

### 20.4.2. dart-developer » Functions » inspect(Object? object) → Object? @6674

> Send a reference to object to any attached debuggers.

### 20.4.3. dart-developer » Functions » log(String message, {DateTime? time, int? sequenceNumber, int level = 0, String name = '', Zone? zone, Object? error, StackTrace? stackTrace}) → void @6675

> Emit a log event, which can can viewed using the DevTools Logging view.

### 20.4.4. dart-developer » Functions » postEvent(String eventKind, Map eventData, {String stream = 'Extension'}) → void @6676

> Post an event of eventKind with payload of eventData to the "Extension" event stream.

### 20.4.5. dart-developer » Functions » registerExtension(String method, ServiceExtensionHandler handler) → void @6677

> Register a ServiceExtensionHandler that will be invoked in this isolate for method. NOTE: Service protocol extensions must be registered in each isolate.

### 20.5.0. dart-developer » Typedefs » ServiceExtensionHandler = Future&lt;ServiceExtensionResponse&gt; Function(String method, Map&lt;String, String&gt; parameters) @6678

> A service protocol extension handler. Registered with registerExtension.

### 20.5.1. dart-developer » Typedefs » TimelineAsyncFunction = Future Function() @6679

> 

### 20.5.2. dart-developer » Typedefs » TimelineSyncFunction&lt;T&gt; = T Function() @6680

> A typedef for the function argument to Timeline.timeSync.

