# package-vm_service_vm_service_io

Estás en el nodo 150 de 161 [93%]

## Índice de package-vm_service_vm_service_io

- Estructura de la documentación:
   - 150.0.0. package-vm_service_vm_service_io » Functions » vmServiceConnectUri(String wsUri, {Log? log}) → Future&lt;VmService&gt; @9649
   - 150.0.1. package-vm_service_vm_service_io » Functions » vmServiceConnectUriWithFactory&lt;T extends VmService&gt;(String wsUri, {required VmServiceFactory&lt;T&gt; vmServiceFactory, Log? log}) → Future&lt;T&gt; @9650
### 150.0.0. package-vm_service_vm_service_io » Functions » vmServiceConnectUri(String wsUri, {Log? log}) → Future&lt;VmService&gt; @9649

> Connect to the given uri and return a new VmService instance.

### 150.0.1. package-vm_service_vm_service_io » Functions » vmServiceConnectUriWithFactory&lt;T extends VmService&gt;(String wsUri, {required VmServiceFactory&lt;T&gt; vmServiceFactory, Log? log}) → Future&lt;T&gt; @9650

> Connect to the given uri and return a new instance of T, which is constructed by vmServiceFactory and may be a subclass of VmService.

