# package-clock_clock

Estás en el nodo 46 de 161 [29%]

## Índice de package-clock_clock

- Estructura de la documentación:
   - 46.0.0. package-clock_clock » Classes » Clock @8563
   - 46.1.0. package-clock_clock » Properties » clock → Clock @8564
   - 46.1.1. package-clock_clock » Properties » now → DateTime @8565
   - 46.2.0. package-clock_clock » Functions » getStopwatch() → Stopwatch @8566
   - 46.2.1. package-clock_clock » Functions » systemTime() → DateTime @8567
   - 46.2.2. package-clock_clock » Functions » withClock&lt;T&gt;(Clock clock, T callback(), {bool isFinal = false}) → T @8568
   - 46.3.0. package-clock_clock » Typedefs » TimeFunction = DateTime Function() @8569
### 46.0.0. package-clock_clock » Classes » Clock @8563

> A provider for the "current time" and points relative to the current time.

### 46.1.0. package-clock_clock » Properties » clock → Clock @8564

> The default implementation of clock for the current Zone. no setter

### 46.1.1. package-clock_clock » Properties » now → DateTime @8565

> Returns the current time as reported by clock. no setter

### 46.2.0. package-clock_clock » Functions » getStopwatch() → Stopwatch @8566

> Returns a stopwatch that uses the current time as reported by clock.

### 46.2.1. package-clock_clock » Functions » systemTime() → DateTime @8567

> Returns the current system time.

### 46.2.2. package-clock_clock » Functions » withClock&lt;T&gt;(Clock clock, T callback(), {bool isFinal = false}) → T @8568

> Runs callback with the given value for the top-level clock field.

### 46.3.0. package-clock_clock » Typedefs » TimeFunction = DateTime Function() @8569

> Returns current time.

